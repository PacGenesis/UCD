import sys

assetName = sys.argv[0];
force = sys.argv[1];
assetId = "WebSphere:assetname=" + assetName

if (len(sys.argv) > 2) :
    assetId = assetId + ",assetversion=" + sys.argv[2];


AdminTask.deleteAsset('[ -assetID "' + assetId + '" -force "' + force + '" ]');
AdminConfig.save();
