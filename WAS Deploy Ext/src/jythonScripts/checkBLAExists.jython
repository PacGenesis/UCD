import sys

name = sys.argv[0]
blaId = "WebSphere:blaname=" + name
if (len(sys.argv) > 1) :
    blaId = blaId + ",blaedition=" + sys.argv[1];

blaList = AdminTask.listBLAs('[ -blaID "' + blaId + '" ]');
if (len(blaList) > 0):
    sys.exit(0)
else:
    sys.exit(30)
