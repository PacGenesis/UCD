import sys;

clusterName = sys.argv[0];
memberName = sys.argv[1];
nodeName = sys.argv[2];
parameters = "[ -clusterName '${clusterName}' -memberConfig [[ '${nodeName}' '${serverName}' '' '' true]]]"
parameters = parameters.replace("${clusterName}", clusterName);
parameters = parameters.replace("${serverName}", memberName);
parameters = parameters.replace("${nodeName}", nodeName);

AdminTask.createClusterMember(parameters);
AdminConfig.save(); 
